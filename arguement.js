function different(num1, num2) {
  console.log("Age is ", num1, num2);
  let diff = num1 - num2;
  console.log("Different is = ", diff);
}

let fatherAge = 34;
let sonAge = 20;

different(fatherAge, sonAge);
